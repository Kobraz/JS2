function getPostsHtml(data) {
    //
    const postContainer = document.createElement('div');
    const headerContainer = document.createElement('header');

    postContainer.append(headerContainer);
    const title = "Hello"

    const post = `
    <div>${title}</div>
    `

}